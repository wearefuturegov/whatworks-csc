.container.content_page.blog

  %h1 Blog and News

  %section.collection.border.blog_list.indented
    - @blog_posts.each_with_index do |b, i|
      .single{class: i == 0 ? "featured" : ""}
        =link_to blog_post_path(id: b.slug) do
          %h3=b.title
        = b.decorate.decorate_image
        .sub_text
          =b.decorate.decorate_author(:short, false)
          .date=b.decorate.decorate_date
        
        = b.decorate.type_tag
